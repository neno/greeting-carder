import { passwordMatchValidator } from './password-match.validator';

describe('passwordMatchValidator', () => {
  it('should create an instance', () => {
    expect(passwordMatchValidator).toBeTruthy();
  });
});
